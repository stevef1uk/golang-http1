apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/stevef1uk/golang-http1:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/stevef1uk/golang-http1:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: golang-http1
        chartPath: charts/golang-http1
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/stevef1uk/golang-http1"
          image.tag: "{{.TAG}}"
        